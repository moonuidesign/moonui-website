export type FAQItem = {
  question: string;
  answer: string; // Placeholder content since original HTML didn't have answers
};

export type FAQCategory = {
  categoryName: string;
  items: FAQItem[];
};
